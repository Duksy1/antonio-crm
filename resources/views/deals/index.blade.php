@extends('layouts.app')
@section('title','Dealovi')
@section('content')
<x-page-header eyebrow="PIPELINE" title="Dealovi" description="Pratite vrijednost i momentum svake prilike."><a class="primary-btn" href="{{ route('deals.create') }}">＋ Novi deal</a></x-page-header>
<x-view-toolbar :list-route="route('deals.index')" :board-route="route('deals.board')" filter-name="stage" :options="App\Enums\DealStage::cases()" placeholder="Pretraži dealove…" />
<div class="entity-grid">@forelse($deals as $deal)<a class="entity-card" href="{{ route('deals.show',$deal) }}"><div class="entity-card-head"><div class="entity-identity"><span class="entity-avatar blue">↗</span><div><h3>{{ $deal->title }}</h3><p>{{ $deal->company?->name ?? 'Bez tvrtke' }}</p></div></div><x-status :status="$deal->stage" /></div><div class="entity-card-body"><div class="meta-row"><span>Vrijednost</span><strong>{{ $deal->currency }} {{ number_format($deal->value,0,',','.') }}</strong></div><div class="meta-row"><span>Vjerojatnost</span><strong>{{ $deal->probability }}%</strong></div></div><div class="entity-card-footer"><span>{{ $deal->expected_close_date?->format('d.m.Y.') ?? 'Bez roka' }}</span><span>{{ $deal->quotes_count }} ponuda →</span></div></a>@empty<x-empty title="Nema pronađenih dealova" text="Dodajte prvu prodajnu priliku."><a class="primary-btn" href="{{ route('deals.create') }}">Dodaj deal</a></x-empty>@endforelse</div><div class="pagination-wrap">{{ $deals->links() }}</div>
@endsection
